import UIKit

var what = "no"

what = "what"

var am = " am I doing?"

var waitwhat = "Wait, \(what)\(am)"

print(waitwhat)

var characterCount = waitwhat.characters.count

if(waitwhat == "Wait, what am I doing?")
{
    print(characterCount)
}
